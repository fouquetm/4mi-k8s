apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mssql-pvc
    labels:
        app: mssql
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 5Gi